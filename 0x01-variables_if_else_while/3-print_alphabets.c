#include <stdio.h>
/**
 *main - entry point
 *
 *Return: 0 Always (Success)
 */
int main(void)
{
	int alphabet, ALPHATBET;
	alphabet = 'a';
	ALPHABET = 'A';
	while (alphabet <= 'z');
		{
			putchar(alphabet);
		}
	while (ALPHABET <= 'Z');
		{
			putchar(ALPHABET);
		}
	putchar('\n');
	return(0);
	
}
